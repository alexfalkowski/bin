#!/usr/bin/env bash
# Refuse local commit operations on master.

set -eo pipefail

if [[ $(git branch --show-current) == master ]]; then
  printf 'bin: refusing to commit on master; create a branch first\n' >&2
  exit 1
fi
