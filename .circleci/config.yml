version: 2.1

jobs:
  build:
    docker:
      - image: alexfalkowski/go:1.18
    steps:
      - checkout
      - run: make lint
    resource_class: large
  release:
    docker:
      - image: alexfalkowski/release:2.0
    steps:
      - checkout
      - run: release

workflows:
  bin:
    jobs:
      - build
      - release:
          context: gh
          requires:
            - build
          filters:
            branches:
              only: master
