interface:
  display_name: "Project Gaps Find"
  short_description: "Find scoped project workflow gaps"
  default_prompt: "Use $project-gaps-find for scoped build, CI, Makefile, release, setup, dependency, validation, and workflow gaps. Follow repo archetype lead generation, apply the shared delegation gate, summarize ledger entries in a compact Title/What/Why table, classify implementation home as current repo, shared bin, external repo, or mixed, resolve entry ID follow-ups through ../project-gaps-implement/ledger.yaml, and never edit files — hand agreed entries to $project-gaps-implement."
