interface:
  display_name: "Reliability Gaps Find"
  short_description: "Find scoped SRE readiness gaps"
  default_prompt: "Use $reliability-gaps-find to find scoped operability, overload, observability, recovery, and data-integrity gaps without editing. After scoping, use repo archetype lead generation, collect exact-revision CircleCI evidence with the shared gap-workflow collector when configured, otherwise use the repository-owned evidence path, reuse exact-state green CI evidence, apply the shared delegation gate, and hand confirmed entries to $reliability-gaps-implement."
