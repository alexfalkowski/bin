interface:
  display_name: "Project Workflow"
  short_description: "Discover commands, CI, and bin wiring"
  default_prompt: "Use $project-workflow to discover project entrypoints, CI expectations, ./bin wiring, and shared Makefile fragment behavior before work begins."
