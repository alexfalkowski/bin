interface:
  display_name: "Project Gaps Implement"
  short_description: "Implement agreed project-gap fixes"
  default_prompt: "Use $project-gaps-implement only after Start <ID> or Approved <ID>-N (or a same-prefix batch) for a scoped project-gap ledger entry. Re-check the entry against current Makefiles and CI before proposing a fix, present the decision card, apply the shared delegation gate, resolve entry ID follow-ups through ledger.yaml, and implement only after explicit agreement to a specific proposal."
