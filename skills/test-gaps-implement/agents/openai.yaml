interface:
  display_name: "Test Gaps Implement"
  short_description: "Implement agreed test-gap fixes"
  default_prompt: "Use $test-gaps-implement only after Start <ID> or Approved <ID>-N (or a same-prefix batch) for a scoped test-gap ledger entry. Re-check the entry against current tests before proposing a fix, present the decision card, apply the shared delegation gate, resolve entry ID follow-ups through ledger.yaml, and implement only after explicit agreement to a specific proposal."
