---
name: test-gaps
description: Deprecated compatibility alias for $test-gaps-find. Use it to preserve existing test-gap audit invocations while migrating to the explicit find workflow.
---

# Test Gaps (Deprecated)

`$test-gaps` preserves the legacy workflow while new prompts migrate to the
explicit names:

- For `Start <ID>`, `Approved <ID>-N`, `Done <ID>`, or `Implement $test-gaps`,
  read and follow `../test-gaps-implement/SKILL.md` completely.
- For Find-mode requests and all other `$test-gaps` invocations, read and follow
  `../test-gaps-find/SKILL.md` completely.

The selected canonical skill owns the workflow's scope, ledger contract,
delegation, and approval gates.
