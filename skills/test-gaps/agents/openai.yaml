interface:
  display_name: "Test Gaps"
  short_description: "Find weak tests and harness gaps"
  default_prompt: "Use $test-gaps for scoped missing, weak, misleading, flaky, wrong-layer, or harness coverage gaps. Follow its active plan, apply the shared delegation gate, preserve confidence/validation/bin/** rules, verify front-door and repository-owned behavior, store TESTS.md, answer test gap ID follow-ups such as TEST-1, and fix one agreed gap."
