---
name: doc-gaps
description: Deprecated compatibility alias for $doc-gaps-fix. Use it to preserve existing documentation-gap invocations while migrating to the default find-and-fix workflow.
---

# Doc Gaps (Deprecated)

`$doc-gaps` preserves the legacy workflow while new prompts migrate to the
explicit names:

- For `Audit-only $doc-gaps` or a request to find doc gaps without editing,
  read and follow `../doc-gaps-audit/SKILL.md` completely.
- For all other `$doc-gaps` invocations, read and follow
  `../doc-gaps-fix/SKILL.md` completely.

The selected canonical skill owns the workflow's scope, ledger contract,
delegation, and approval gates.
