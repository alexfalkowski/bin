#!/usr/bin/env bash
# shellcheck disable=SC1091
# Collects exact-revision CircleCI evidence for the shared gap workflow.

set -eo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$script_dir/../../../lib/skills/collector.sh"

run_skill_collector 'gap-workflow-ci' "$script_dir/collect-ci.rb" "$@"
